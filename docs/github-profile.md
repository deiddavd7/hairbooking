# GitHub Repository Setup

Suggested repository description:

```text
iOS SwiftUI booking management app for salons and small studios, with scheduling, reports, no-show risk, XCTest and GitHub Actions.
```

Suggested topics:

```text
swiftui, ios, booking-system, scheduling, xctest, github-actions, thesis-project, portfolio, mvvm, local-first
```

Suggested website field:

```text
https://github.com/deiddavd7/hairbooking
```

Suggested social preview:

- Use `docs/assets/dashboard-preview.svg` or export it as PNG.
